// Online C compiler to run C program online
#include <stdio.h>
#include<string.h>

int main() {
   int first[100],second[100],temp[100];
   printf("\nENter the first string");
  scanf("%s",&first);
   printf("\nENter the second string");
  scanf("%s",&second);
   printf("\nThe first string is= ");
  printf("%s",first);
   printf("\nThe second string is=");
   printf("%s",second);
   strcpy(temp,first);
   strcpy(first,second);
   strcpy(second,temp)
   printf("After the swapping ");
   pritnf("%s",first);
   printf("%s",second);
   
    return 0;
}
